// RequestTradeSignalCard has been removed – dashboard no longer supports manual signal requests.
// The component is kept as a no-op export to avoid import errors in legacy layouts.

export function RequestTradeSignalCard() {
  return null;
}
